const LOCALPATH = [] ; // 文件路径 eg: ['Dist', 'static']
const UPYUNCONFIG = ['your service name', 'your operator name', 'your operator password']; // 又拍云服务


module.exports = {
    localPathConfig: LOCALPATH,
    upyunConfig: UPYUNCONFIG
}